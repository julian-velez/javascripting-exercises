// Define the variables
let total = 0;
let limit = 10;

// Create a for loop that runs while i is less than limit
for (let i = 0; i < limit; i++) {
  // Add i to the total on each iteration
  total += i;
}

// Print the total to the console
console.log(total);
