// Define the array
let pets = ['cat', 'dog', 'rat'];

// Loop through each element and make them plural
for (let i = 0; i < pets.length; i++) {
  pets[i] = pets[i] + 's';  // Append 's' to each string
}

// Print the updated array to the terminal
console.log(pets);
